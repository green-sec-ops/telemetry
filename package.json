{
  "name": "action",
  "version": "0.1.0",
  "private": true,
  "description": "GreenSecOps telemetry collection action for GitHub Actions",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "ncc build src/pre.ts -o dist/pre --minify --no-source-map-register && ncc build src/main.ts -o dist/main --minify --no-source-map-register && ncc build src/post.ts -o dist/post --minify --no-source-map-register && ncc build src/daemon.ts -o dist/daemon --minify --no-source-map-register",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --write --unsafe --no-errors-on-unmatched --files-ignore-unknown=true src/",
    "generate-client": "openapi-ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/node": "^20.17.57",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.8.3"
  }
}
